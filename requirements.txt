# Pinned Python dependencies for the native WSL2 evaluation environment.
# Create/activate `.venv` with Python 3.11, install the CUDA PyTorch and
# llama-cpp-python wheels separately, then install this file.
#
# These versions match the environment used by the preserved current-schema
# finalist runs. The lm-eval revision matches the local upstream clone.
#
# torch is NOT in this file. PyTorch's official install pattern uses
#   uv pip install --python .venv/bin/python torch==2.6.0 \
#     --index-url https://download.pytorch.org/whl/cu124
# which restricts pip to *only* PyTorch's wheel index. Mixing it via
# --extra-index-url risks pip picking the wrong wheel for a sibling package.
# Install it separately for this reason.
#
# llama-cpp-python is also NOT here. Install the verified CUDA wheel separately:
#   uv pip install --python .venv/bin/python llama-cpp-python==0.3.20 \
#     --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu124

transformers==5.6.2
accelerate==1.13.0
huggingface_hub==1.12.0
lm_eval[gguf] @ git+https://github.com/EleutherAI/lm-evaluation-harness.git@9b2b9280330a3a5b20953346c8b51e23c4c8c4e2
